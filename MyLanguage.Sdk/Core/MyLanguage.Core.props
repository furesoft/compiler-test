<Project>

  <PropertyGroup>
    <DefaultLanguageSourceExtension>.my</DefaultLanguageSourceExtension>
  </PropertyGroup>

  <Import Project="MyLanguage.ProjectSystem.props"/>

  <PropertyGroup Condition="@(Configuration) == 'Release'">
    <Optimize>true</Optimize>
  </PropertyGroup>

</Project>