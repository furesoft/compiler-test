<Project>

    <PropertyGroup Condition="'$(MSBuildProjectExtension)' == '.myproj'">
        <LanguageTargets>$([MSBuild]::NormalizePath('$(MSBuildThisFileDirectory)', '..\Core\MyLanguage.Core.targets'))</LanguageTargets>
    </PropertyGroup>

    <Import Sdk="Microsoft.NET.Sdk" Project="Sdk.targets"/>

</Project>